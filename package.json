{
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=9.5",
    "yarn": "^1.3.2"
  },
  "resolutions": {
    "kind-of": ">=6.0.3"
  },
  "scripts": {
<<<<<<< HEAD
    "start": "cd frontend && yarn start",
    "build": "npm-run-all --parallel build:*",
    "build:frontend": "cd frontend && yarn build",
    "test": "npm-run-all --parallel test:*",
    "test:frontend": "cd frontend && yarn test",
    "test:backend": "cd backend && cargo test",
    "check": "yarn pretty:check",
    "check:frontend": "cd frontend && yarn pretty:check",
    "check:backend": "cd backend && cargo check"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
=======
    "build": "yarn build:common && yarn build:frontend",
    "build:common": "tsc -p packages/common",
    "build:frontend": "cd ./packages/frontend && yarn run build && cp -r ./build ../../htdocs",
    "start:frontend": "yarn build:common && cd packages/frontend && yarn start",
    "check:common": "tsc -p packages/common --noEmit",
    "deploy": "yarn build:all && pm2 restart index",
    "test": "yarn && tsc -p packages/common && node packages/common/test | tap-spec && cd packages/frontend"
>>>>>>> 3adacb8... Switch the image to nginx with ENV support
  }
}
